spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: recursia2005
    url: jdbc:postgresql://localhost:5432/postgres
  jpa:
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
    show-sql: true
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.yaml
    enabled: true
logging:
  level:
    liquibase: DEBUG
jwt:
  secret: "bXlTdXBlclNlY3JldEtleTEyMzQ1Njc4OTAxMjM0NTY="  # сгенерированный ключ
  expiration: 86400000
  security:
    user:
      name: admin       # если нужен дефолтный пользователь (необязательно)
      password: admin123
#springdoc:
#  api-docs:
#    path: /api-docs/v3/api-docs
#  swagger-ui:
#    path: /api-docs/swagger-ui.html
#    tags-sorter: alpha